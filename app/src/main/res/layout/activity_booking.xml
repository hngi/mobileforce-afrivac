<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="16dp"
    android:orientation="vertical"
    tools:context=".BookingActivity">

    <RelativeLayout
        android:id="@+id/logo"
        android:layout_width="wrap_content"
        android:layout_height="150dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp">

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/logo_back"
            android:contentDescription="@string/app_name"
            android:paddingStart="3.5dp"
            android:paddingTop="32.5dp"
            android:paddingEnd="3.5dp"
            android:paddingBottom="32.5dp"
            android:src="@drawable/afri_vac" />

    </RelativeLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/logo"
        android:text="@string/hotel_booking"
        android:textSize="30sp"
        android:textStyle="bold" />


    <LinearLayout
        android:id="@+id/dates"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:labelFor="@id/start_date"
            android:text="@string/start_date"
            android:textSize="16sp" />

        <EditText
            android:id="@+id/start_date"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginTop="10dp"
            android:autofillHints="@string/start_date"
            android:background="@drawable/input_border"
            android:inputType="date" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:labelFor="@id/end_date"
            android:text="@string/end_date"
            android:textSize="16sp" />

        <EditText
            android:id="@+id/end_date"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginTop="10dp"
            android:autofillHints="@string/end_date"
            android:background="@drawable/input_border"
            android:inputType="datetime" />
    </LinearLayout>

    <TextView
        android:id="@+id/guestTextView"
        android:layout_width="240dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/dates"
        android:layout_marginTop="16dp"
        android:labelFor="@id/guests"
        android:text="@string/guests"
        android:textSize="16sp" />

    <EditText
        android:id="@+id/guests"
        android:layout_width="240dp"
        android:layout_height="40dp"
        android:layout_below="@id/guestTextView"
        android:layout_marginTop="10dp"
        android:autofillHints="@string/guests"
        android:background="@drawable/input_border"
        android:inputType="number" />

    <TextView
        android:id="@+id/roomsTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/dates"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:layout_toEndOf="@id/guestTextView"
        android:labelFor="@id/rooms"
        android:text="@string/rooms"
        android:textSize="16sp" />

    <EditText
        android:id="@+id/rooms"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_below="@id/roomsTextView"
        android:layout_marginStart="16dp"
        android:layout_marginTop="10dp"
        android:layout_toEndOf="@id/guests"
        android:autofillHints="@string/rooms"
        android:background="@drawable/input_border"
        android:inputType="number" />

    <Button
        android:layout_width="311dp"
        android:layout_height="62dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="30dp"
        android:background="@drawable/button_border"
        android:text="@string/next"
        android:textColor="#fff"
        android:textStyle="bold" />


</RelativeLayout>